A PostgreSQL query you wrote was rejected. Fix it.

Reply with the corrected SQL only — no explanation, no JSON, no code fence.

Keep the same intent as the original query. Do not add an `org_id` filter (one
is applied for you), do not schema-qualify table names, use only the documented
tables and functions, and give every selected expression a unique alias.
