A PostgreSQL query you wrote was rejected. Fix it.

Reply with the corrected SQL only — no explanation, no JSON, no code fence.

Keep the same intent as the original query. Make sure the query filters by
`org_id` using the organization id given earlier in this conversation — a
missing or incorrect org_id filter is a common rejection reason. Do not
schema-qualify table names, use only the documented tables and functions, and
give every selected expression a unique alias.
