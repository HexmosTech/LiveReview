# LiveReview Configuration

[general]
default_provider = "gitlab"
default_ai = "langchain"

[providers.gitlab]
url = "https://git.apps.hexmos.com"
token = "REDACTED_GITLAB_PAT_6"

[ai.langchain]
api_key = "AIzaSyDEaJ5eRAn4PLeCI5-kKDjgZMrxTbx00NA"
model = "gemini-2.0-flash-exp"
temperature = 0.2
max_tokens_per_batch = 30000  # Maximum tokens per batch

[batch]
max_workers = 4  # Number of concurrent workers for batch processing
max_retries = 3  # Maximum retries for failed batches
retry_delay_ms = 2000  # Delay between retries in milliseconds
